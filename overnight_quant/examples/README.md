# Example State

This directory contains committed demonstration outputs only. Commands run with `--mode demo` write to `examples/records/` and `examples/reports/`.

Real trading-day commands do not read these files. To record or review a teaching example, pass `--state example` explicitly.
